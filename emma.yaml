pipeline{
	agent any
	stages{
		stage('1-git-clone'){
			steps{
				sh 'git --version'
				sh 'git clone'
			}
		}
		stage('2-mavenbuild'){
			steps{
				echo 'Am a devop engineer'
			}
		}
	}
}
